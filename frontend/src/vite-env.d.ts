/// <reference types="vite/client" />

// Штатные типы Vite. Раньше их не было вовсе, и любой vite-специфичный импорт
// (`?raw`, `import.meta.env`) не проходил проверку типов — притом что в
// рантайме работал. Понадобилось для теста, который читает index.html как
// строку: правило «фон только на body» живёт в разметке, а не в модуле, и
// проверить его больше неоткуда.
