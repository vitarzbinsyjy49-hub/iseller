/** Подпись и бегущие подсказки строки «Найти технику».
 *
 *  ОДНИ на главную и на desktop-шапку — как и маршруты строки (searchRoutes).
 *  Это одно и то же поле в двух вёрстках, и разъехаться его подсказкам негде
 *  только пока текст лежит в одном месте. В этом проекте уже расходились списки
 *  категорий, разнесённые по четырём файлам.
 */

/** Подпись поля. Короткая НЕ случайно: рядом стоит кнопка AI, и на 390px полю
 *  остаётся ~147px. Прежние «Найти iPhone, MacBook, AirPods…» обрывались на
 *  середине слова, а обрезанная подсказка хуже короткой. */
export const SEARCH_PLACEHOLDER = "Найти технику";

/** Что строка печатает сама, пока её не тронули.
 *
 *  КАЖДАЯ фраза проверена на непустую выдачу через настоящий путь поиска —
 *  словарь алиасов в `api/catalog.py` переводит разговорное слово в название
 *  товара: «айфон» → iphone (151), «макбук» → macbook (50), «дайсон» → dyson
 *  (50), «часы» → watch (18). Подсказка, ведущая в пустой экран, — обещание,
 *  которого магазин не выполняет.
 *
 *  Названий КАТЕГОРИЙ здесь нет, и это не стилистика: «смартфоны» и «ноутбуки»
 *  в алиасы не входят и дают РОВНО НОЛЬ результатов — текстовый поиск идёт по
 *  названиям товаров и брендам, а не по разделам. Проверено на живой базе.
 *
 *  «Дайсон» стоит здесь намеренно: без него строка выглядела бы поиском по
 *  одному бренду, хотя магазин продаёт не только Apple. */
export const SEARCH_HINTS = ["айфон", "макбук", "дайсон", "часы"];
